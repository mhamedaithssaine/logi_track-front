import { Routes } from '@angular/router';

/**
 * Routes pour le module Client
 * TODO: Implémenter les routes client (produits, commandes, réservation, tracking)
 */
export const routes: Routes = [
  {
    path: '',
    redirectTo: 'dashboard',
    pathMatch: 'full',
  },
  // TODO: Ajouter les routes client
];

