import { Routes } from '@angular/router';

/**
 * Routes pour le module Warehouse Manager
 * TODO: Implémenter les routes warehouse (inventaire, mouvements, réservations, expéditions)
 */
export const routes: Routes = [
  {
    path: '',
    redirectTo: 'dashboard',
    pathMatch: 'full',
  },
  // TODO: Ajouter les routes warehouse
];

